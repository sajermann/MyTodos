services:
  my-todos:
    container_name: MyTodos
    restart: always
    build: .
    ports:
      - '8000:80'
    networks:
      - fast-deploy
networks:
  fast-deploy:
    driver: bridge
    name: fast-deploy
